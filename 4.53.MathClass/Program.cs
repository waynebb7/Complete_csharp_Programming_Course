namespace _4._53.MathClass
{
    internal class Program
    {
        static void Main(string[] args)
        {
            System.Diagnostics.Debug.WriteLine(Math.Abs(-3));
            System.Diagnostics.Debug.WriteLine(Math.Round(-3.5f));
            System.Diagnostics.Debug.WriteLine(Math.Ceiling(3.5f));
            System.Diagnostics.Debug.WriteLine(Math.Floor(3.5f));
            System.Diagnostics.Debug.WriteLine(Math.Truncate(3.5f));
            System.Diagnostics.Debug.WriteLine(Math.Min(3,1));
            System.Diagnostics.Debug.WriteLine(Math.Max(3,1));
        }
    }
}